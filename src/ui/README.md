# ui

Contains logic for the GUI.

**Notes:**

- The foundational logic for the application window is in the `main_window.py` file.
- The application windows are denoted as 'pages' which are confined to custom widget classes.
    - These classes are defined in the `*_widget.py` files.